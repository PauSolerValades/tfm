# Ideas to analyze the traces.

Actions:
- validate Ratios per user and global.
- Validate inter_action_duration -> ok
- Num of reposts/likes/nothing per post
- Growth of reposts in a single post.
- structural virality (merge it into a big project)
- Furthest travel (plus furthest actual path)
- % of posts seen/liked/ignored by users n/total, grow session wise (like, a bar per session)

Sessions: 
- validate distribution duration and inter.
- num of sessions per user.
- timeline emptyness in every end of session.
- num of posts seen per every user in every session

Create:
- validate distribution at warmup and at normal.
- total number of posts in the simulation (delta T) 

maybe the best approach would be to compute and store everything in a file, and just run the codes to generate the desired image! (actually, a data visualizatoin tool would be nice but oof)
